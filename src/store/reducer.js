const initialState = {
    top10MoviesLoading: false,
    top10Movies: [],
};

function reducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default reducer;
